[build-system]
requires = ["flot"]
build-backend = "flot.buildapi"

[project]
name = "module3"
version = "1.0"
description = "module3 desc"


[tool.flot]
includes = [ "src/module3.py" ]
metadata_files = [ "LICENSE" ]
editable_paths = [ "src" ]
wheel_path_prefixes_to_strip = [ "module3.py" ]
